<?xml version="1.0" encoding="utf-8"?>
<!-- Per-app language support (Settings > Apps > VortX > Language, Android 13+). English only for
     now: S01 scope is wiring the mechanism, not adding locales; S12 owns full string
     externalization + additional languages per ANDROID-PLAN.md. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
</locale-config>
